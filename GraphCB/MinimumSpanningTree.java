public class MinimumSpanningTree{
    // minimum spanning tree is a graph with if it has n number of vertice then it willbe connected with n-1 edges 
    // and  a graph can have more than one spanning tree 
}