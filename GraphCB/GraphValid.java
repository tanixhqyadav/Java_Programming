import java.util.*;
public class GraphValid{
    public static boolean graph(HashMap<Integer, ArrayList<Integer>> mp){
        Stack<Integer> st=new Stack<>();
        HashSet visited =new HashSet<>();
        for(int nbrs:mp.keySet()){
            st.add(nbrs);
            while(!st.isEmpty()){
                int rv=st.pop();
                if
            }
        }

    }
}